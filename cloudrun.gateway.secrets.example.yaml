# Required secrets for Slack gateway
SLACK_BOT_TOKEN: "xoxb-xxxxxxxx"
SLACK_SIGNING_SECRET: "xxxxxxxx"
BACKLOG_API_KEY: "xxxxxxxx"

# Required when WORK_QUEUE_MODE=http
WORK_SERVICE_TOKEN: "replace-with-the-same-token-used-by-work-service"

# Optional secret
# Set only when Cloud Run should access PostgreSQL directly.
DATABASE_URL: "postgresql://user:password@host:5432/legalbridge"
